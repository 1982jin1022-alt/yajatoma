# ─────────────────────────────────────────────
#  공개 저장소에 올리면 안 되는 것
# ─────────────────────────────────────────────

# 지인들과의 카카오톡 대화 기록 (설정 원출처). 사적인 대화라 절대 올리지 말 것
ppp/

# 작업 중 찍은 화면 캡처
문제/

# ─────────────────────────────────────────────
#  올릴 필요가 없는 것
# ─────────────────────────────────────────────

# Claude 아티팩트 발행용 조각 파일. Pages에서는 index.html 을 쓴다
야자_설정집.html
커미션_시트.html

# 커미션 원본 51MB + 웹용 리사이즈본(yt/web). 웹에 나가는 사본은 images/ 에 있으므로 yt/ 는 통째로 제외
yt/

# ─────────────────────────────────────────────
#  반드시 커밋해야 하는 것 (참고)
#    index.html, commission/index.html, images/
#    images/ 가 빠지면 사이트의 그림이 전부 깨진다
# ─────────────────────────────────────────────

# 개인 창작물 원고(회지 본문 등). 저장소와 무관하고 공개 대상도 아니다
이것저것/
